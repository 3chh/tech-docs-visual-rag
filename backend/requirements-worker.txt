# PDF worker: layout detection + OCR + ghép ảnh-mục
#
# paddlepaddle KHÔNG cài bằng pip ở đây: index wheel của Paddle
# (paddlepaddle.org.cn/packages/stable/cuXXX/) render bằng JS nên pip không
# thấy link nào, còn PyPI chỉ có paddlepaddle-gpu tới 2.6.2. Paddle đến từ
# base image trong Dockerfile.worker, đã khớp CUDA và cuDNN.
-r requirements-base.txt

paddleocr==3.1.1
paddlex==3.1.4
pdf2image==1.17.0
PyMuPDF==1.25.1
opencv-python-headless==4.10.0.84
scipy==1.15.0
numpy<2.0
